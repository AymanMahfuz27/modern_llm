# Configuration Files

- Store experiment configs as JSON, YAML, or Python dataclasses.
- Prefer small, composable configs (e.g., `lm_small.yaml`, `sst2_lora.yaml`).
- Scripts accept a `--config` flag pointing to these files.

